#include <iostream>
#include <bitset>
#include <queue>
#include <map>
#include <string>
#include <cstdio>
using namespace std;

int main() {
	// vector<int> arr = {1,2,3,4}; // 주어진 집합
	// int size = arr.size();
    
    // for(int i = 1; i < (1 << size) / 2; i++) {
    //     int aStatus = 0;
    //     int bStatus = 0;
    //     cout << i << "|\t";
    // 	for(int j = 0; j < size; j++) {
    //     	if(i & (1 << j)) {
    //         	cout << arr[j] << " ";
    //             aStatus |= (1 << j);
    //         }
    //     } 
    //     cout << " : ";
    // 	for(int j = 0; j < size; j++) {
    //     	if(~i & (1 << j)) {
    //         	cout << arr[j] << " ";
    //             bStatus |= (1 << j);
    //         }
    //     } 
    //     cout << "\t(" << aStatus << ", " << bStatus << ")\n\n";
    // }

    bool test = false;
    test |= true;
    cout << test;
}